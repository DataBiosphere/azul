language: python

sudo: required

services:
  - docker

python:
  - 3.6

before_install:
  - (cd deployments && ln -snf dev .active)
  - source environment

install:
  - pip install -r requirements.dev.txt

script:
  - source environment
  - make test
