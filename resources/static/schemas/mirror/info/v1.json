{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "title": "info",
    "description": "Information describing a file mirrored by Azul",
    "type": "object",
    "properties": {
        "content-type": {
            "type": "string",
            "description": "Content type of the mirrored file, as defined for the HTTP response header of the same name"
        },
        "$schema": {
            "type": "string",
            "format": "uri",
            "pattern": "^https?://.*/info/v\\d+\\.json$",
            "description": "URL of a JSON schema the JSON containing this property is valid against"
        }
    },
    "required": [
        "content-type",
        "$schema"
    ]
}